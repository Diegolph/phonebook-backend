# MongoDB Atlas database user, not your Atlas account email.
MONGODB_USER=YOUR_DATABASE_USER
# Only the hostname after @ and before / in the Atlas connection string.
MONGODB_HOST=cluster0.example.mongodb.net
# Exercise 3.12 receives the password as a command-line argument.
